function integ() {
    let number=prompt("Your number?");
    if(number>0)
    {
        document.write("Bigger zero");
    }
    else if(number==0)

    {
        document.write("Equal zero");
    }
    else
    {
        document.write("Smaller zero");
    }
}